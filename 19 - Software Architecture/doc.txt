Ciao Davide, ho appena letto la mail e ti ho trovato semplicemente cercando tra i componenti del gruppo di Software Architecture ho visto il tuo nome e cognome e per rispondere alla tua domanda noi abbiamo semplicemente creato un readme all'interno della root del progetto in GitHub quindi sostanzialmente non appena noi, cioè non appena il prof entra nel repository si trova un readme con delle istruzioni su come avviare i back-end noi abbiamo gestito tutto con il docker compose quindi sostanzialmente quello che andiamo a dirgli è guarda vai su monolith application, distributed application in base a quello che vuole avviare e poi fai Docker Compose App, ok?
Poi abbiamo mostrato quali sono gli URL principali, dato che comunque abbiamo delle API, non so se le avete documentate con uno Swogger OpenAPI qualche modo insomma poi se ci sono di front-end diversi che ne so ad esempio un'interfaccia admin abbiamo solo linkato i url principali e poi abbiamo anche fornito i dati di authentication noi abbiamo avevamo due front-end uno per l'admin e uno per il player e quindi non appena non appena tu esegui il comando di prima, quindi il docker-compose-app, lui in realtà, il programma, cosa fa. Perché altrimenti è dura da far vedere insomma come funziona e se funziona. Noi abbiamo creato degli utenti, abbiamo popolato un po' il DB in modo tale da mostrare che, sì, un'applicazione nel bel mezzo di qualche torneo e qualche partita. e quindi abbiamo fornito username e password sia dell'admin sia di un player così il prof può provare entrambi i frontend e può verificare che tutto funziona e poi quello che va a fare quello che abbiamo linkato è tutta la documentazione un po' più tecnica quindi abbiamo messo dei link sempre a dei readme infatti dentro la folder monolith distribuite e abbiamo creato altri ridmi e abbiamo semplicemente linkato questa documentazione qua, sono semplici ridmi. Cos'altro ti posso dire?
Cioè noi abbiamo anche sfruttato un po' l'AI perché comunque a fare queste cose qua è brava, poi abbiamo corretto un po' perché ha anche scritto castronerie, però in generale abbiamo anche descritto come sia la monolithic che noi abbiamo usato la modular monolith, mi pare anche a voi qual è la struttura delle cartelle e cos'altro come puoi lanciare i test anche se al prof non credo che interessi più di tanto abbiamo scritto un attimo i vantaggi e svantaggi rispetto sia a quella distribuita ok e per la distribuita uguale Poi, un attimo, l'architettura. Noi abbiamo fatto anche dei diagrammini. I diagrammini, comunque, ricordano un po' quello che abbiamo seguito a lezione sulle slide. Quindi, semplicemente, se ti prendi l'architettura distribuita e la monolitica, ti prendi una cosa di quel genere, i diagrammi là e replichi un attimo sul vostro contesto, quello che avete fatto ad esempio ci sono due frontend, prendi due rettangoli e scrivi frontend admin, frontend client c'è un proxy, mettete il proxy ci sono tre backend, mettete tre backend c'è un unico backend, comunque sulle slide ci sono E poi niente, secondo me quando avete spiegato queste cosette qua siete anche a posto. Ah, poi ecco l'ultima cosa, considerazioni finali su anche tutte le caratteristiche architetturali, quindi le Structure, le Characteristics, le Engineering e le Operational.